% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbInsertSpatial.R
\name{dbInsertSpatial}
\alias{dbInsertSpatial}
\title{Insert Spatial Data into SQL Database}
\usage{
dbInsertSpatial(
  conn,
  spatial.data,
  schema.name = NULL,
  table.name,
  geom.col = "geom_wkt",
  chunk.size = 5000L,
  overwrite = FALSE,
  include.srid = TRUE,
  verbose = TRUE,
  dialect
)
}
\arguments{
\item{conn}{A valid database connection object.}

\item{spatial.data}{An `sf` object to insert.}

\item{schema.name}{Schema name. Defaults based on dialect.}

\item{table.name}{Target table name.}

\item{geom.col}{Name for the geometry column in the database. Default is "geom_wkt".}

\item{chunk.size}{Number of rows per chunk. Default is `5000` (smaller due to WKT size).}

\item{overwrite}{Whether to overwrite existing data. Default is `FALSE`.}

\item{include.srid}{Whether to include SRID in WKT (as EWKT). Default is `TRUE`.}

\item{verbose}{Whether to print progress messages. Default is `TRUE`.}

\item{dialect}{Character string specifying database dialect: "mssql" or "postgres".}
}
\value{
A `data.frame` transaction log.
}
\description{
Inserts a spatial data frame (sf object) into a database table, converting
geometries to WKT text. Supports both SQL Server and PostgreSQL.
}
\examples{
\dontrun{
library(sf)
nc.sf <- st_read(system.file("shape/nc.shp", package = "sf"))
log.df <- dbInsertSpatial(
  conn = conn,
  spatial.data = nc.sf,
  table.name = "nc_counties",
  dialect = "postgres"
)
}
}
